{
  "name": "omp-provider-switcher",
  "private": true,
  "scripts": {
    "test": "bun test",
    "typecheck": "cd web && bunx tsc --noEmit -p tsconfig.json",
    "check:rust": "cargo check --manifest-path tauri/Cargo.toml"
  },
  "dependencies": {
    "yaml": "^2.5.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
